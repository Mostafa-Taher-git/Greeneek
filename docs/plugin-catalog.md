<!-- Generated by scripts/gen-plugin-catalog.ts — do not edit by hand.
     Run `pnpm run gen-plugin-catalog` to regenerate. -->

# Plugin Catalog

Every workspace package (`packages/<family>/<name>`), categorized by what it contributes, so it is easy to tell a Tool from a Skill from a Command and everything else. Kinds follow naming and manifest conventions, checked by the generator — see the legend. Deeper references per kind: the generated Tool Schema Catalog (`docs/tool-catalog.md`) for tool schemas, each `packages/<family>/README.md` group map for family roles, and each package README for the full contract.

## Legend

| Kind | What it is | How the generator detects it |
| --- | --- | --- |
| Tool | Model-facing tools on `ctx.tools` | directory starts with `tool-` (same scope as the tool catalog) |
| Skill | Skill registry, providers, and consumer on `ctx.skills` | `packages/skill/*` |
| Command | Client slash/popup commands (`ctx.commandUi`, input trigger sources) | curated set, guarded: every command-seam marker in source must resolve into it |
| UI | Browser surfaces | directory starts with `ui-` |
| LLM | Model routes and provider adapters | `packages/llm/*` |
| Capability | Every other plugin, grouped by family directory | fallback: anything unmatched |

Scope: 253 workspace packages; every package appears under each of its kinds (22 Tool, 4 Skill, 6 Command, 40 UI, 7 LLM, 181 Capability rows). A package with several kinds (e.g. a tool that also contributes a command) appears in each section.

## Tools

| Package | Role |
| --- | --- |
| `@greeneek/gnk-experimental-tool-agent-team` | Nine tools that let the model create, message, and coordinate teammates, for compositions mounting the experimental Team plugins. |
| `@greeneek/gnk-tool-cordis` | Model-facing Cordis runtime tools for agents and maintainers choosing, composing, or debugging dynamic-package workflows. |
| `@greeneek/gnk-tool-fs` | The model-facing read, read_image, write, and edit tools for users and maintainers composing or debugging filesystem access for agents. |
| `@greeneek/gnk-tool-fs-search` | The model-facing glob and grep discovery tools for users and maintainers composing or debugging workspace search for agents. |
| `@greeneek/gnk-tool-str-replace-editor` | The standalone str_replace_editor tool over ctx.fs for users and maintainers composing Claude-Code-style file editing for agents. |
| `@greeneek/gnk-tool-goal` | The model-facing goal tools for users and maintainers choosing, composing, or debugging get_goal, create_goal, and update_goal. |
| `@greeneek/gnk-tool-ask-user` | The model-facing ask_user_question tool over the user-questions seam, for users and maintainers composing or debugging interactive agent surfaces. |
| `@greeneek/gnk-tool-jobs` | The model-facing background-job controls for users and maintainers choosing, configuring, or debugging job_output, job_list, job_kill, and completion notices. |
| `@greeneek/gnk-tool-lsp` | The model-facing lsp tool: four read-only code-navigation operations with one-based UTF-16 cursor coordinates, bounded results, and hover text, for users and maintainers composing model code navigation. |
| `@greeneek/gnk-tool-session-query` | Workspace-authorized model-facing session history tools for agent developers and maintainers choosing, configuring, or debugging prior-session search, tracing, and event reads. |
| `@greeneek/gnk-tool-bash` | The model-facing bash tool for users and maintainers choosing, configuring, or debugging one-shot command execution, background jobs, and sandbox escalation. |
| `@greeneek/gnk-tool-bash-persistent` | The model-facing persistent bash tool for users and maintainers choosing, configuring, or debugging owner-scoped shell state that survives across calls. |
| `@greeneek/gnk-tool-pwsh` | The model-facing pwsh tool for users and maintainers choosing, configuring, or debugging one-shot PowerShell execution, background jobs, and sandbox escalation on Windows. |
| `@greeneek/gnk-tool-pwsh-persistent` | The model-facing persistent pwsh tool for users and maintainers choosing, configuring, or debugging owner-scoped PowerShell state that survives across calls. |
| `@greeneek/gnk-tool-skill` | The model-facing skill catalog and loader tool for users and maintainers understanding what agents see, or configuring the session skill catalog. |
| `@greeneek/gnk-tool-subagent` | Model-facing subagent delegation tool for users and maintainers configuring, composing, or debugging delegation over a subagent provider. |
| `@greeneek/gnk-tool-subagent-control` | Global send_message, interrupt_agent, and list_agents tools for users and maintainers composing or debugging continuable-child control. |
| `@greeneek/gnk-tool-terminal` | Six model-facing persistent terminal tools with owner isolation, bounded results, and optional background sends for agents that need cross-call terminal state. |
| `@greeneek/gnk-tool-todo` | The model-facing todo_write tool over the Greeneek Harness session log: whole-list replacement, per-session ownership, and the todos projection, for users and maintainers choosing, configuring, or debugging the tool. |
| `@greeneek/gnk-tool-web` | The model-facing web tools (web_search, web_fetch) over ctx.web: how deployments enable, configure, and observe the search and fetch tools the model sees. |
| `@greeneek/gnk-tool-ralph` | The model-facing ralph tool: a fixed foreground fresh-agent loop toward one immutable objective, for users and maintainers choosing or configuring fresh-agent iteration. |
| `@greeneek/gnk-tool-workflow` | The model-facing workflow tool: run a JavaScript orchestration script that fans out subagents, for users and maintainers choosing or configuring model-driven orchestration. |

## Skills

| Package | Role |
| --- | --- |
| `@greeneek/gnk-skill` | The skill provider registry for users and maintainers choosing, configuring, or debugging how skills from any source are merged, resolved, and loaded. |
| `@greeneek/gnk-skill-badge` | The bundled 'powered by gnk' badge skill for users and maintainers enabling, using, or debugging the optional badge provider. |
| `@greeneek/gnk-skill-filesystem` | The local filesystem skill provider for users and maintainers authoring local skills or configuring how project, custom, and user skill roots are discovered and watched. |
| `@greeneek/gnk-tool-skill` | The model-facing skill catalog and loader tool for users and maintainers understanding what agents see, or configuring the session skill catalog. |

## Commands

| Package | Role |
| --- | --- |
| `@greeneek/gnk-client-ui-commands` | Client command API for the Web GUI: the / command source, three dispatch kinds, the per-session command directory, and popupSelect registration for business packages; for users and maintainers of slash commands. |
| `@greeneek/gnk-client-ui-conversation` | Target-neutral conversation assembly and browser shell: event and view registries, per-session bindings, input state, slots, and temporary composer takeovers. |
| `@greeneek/gnk-client-ui-input-trigger` | Input trigger pipeline for the Web GUI: / and @ detection under the caret, the grouped candidate menu, and pick routing to registered sources; for users and maintainers of slash commands and references. |
| `@greeneek/gnk-client-ui-model-selection` | Model selection for the Web GUI: the /model popup and the composer model seat over one per-session provider-grouped directory; for users and maintainers of model routing. |
| `@greeneek/gnk-client-ui-reference` | Web @file and @session reference source for the composer: candidates, ordering, and atomic inline references (unified file/session picking). |
| `@greeneek/gnk-client-ui-skill` | Web skill references and the dedicated skill tool row for the gnk web client: the /-triggered skill source and the skill call card. |

## UI surfaces

| Package | Role |
| --- | --- |
| `@greeneek/gnk-client-ui-agent-preset` | Agent-preset surfaces for the Web GUI: the default-preset setting, the new-session chip, the session-header label, and the preset roster management section; for users and maintainers of agent composition. |
| `@greeneek/gnk-client-ui-approval` | Browser approval UI that answers Host permission requests through the scoped interaction path. |
| `@greeneek/gnk-client-ui-attachment` | Attachment presentation for the conversation UI: draft-image rail, document drop target, history-image gallery, and original-image lightbox; for users and maintainers of the Web attachment experience. |
| `@greeneek/gnk-client-ui-brand-official` | Official Greeneek Harness brand occupants for the sidebar, active only in official builds; for users and maintainers choosing or replacing brand presentation. |
| `@greeneek/gnk-client-ui-chat` | Browser Chat target that renders Session conversation nodes, details, historical images, actions, localization, and scroll state. |
| `@greeneek/gnk-client-ui-commands` | Client command API for the Web GUI: the / command source, three dispatch kinds, the per-session command directory, and popupSelect registration for business packages; for users and maintainers of slash commands. |
| `@greeneek/gnk-client-ui-conversation` | Target-neutral conversation assembly and browser shell: event and view registries, per-session bindings, input state, slots, and temporary composer takeovers. |
| `@greeneek/gnk-client-ui-deliverables` | Produced-files and clickable file references for the Web GUI: the deliverables row a finished turn ends with, and inline-code links in the closing prose; for users and maintainers of the deliverables experience. |
| `@greeneek/gnk-client-ui-directory-picker-browse` | In-app directory-browsing surface: the Miller-column Select Workspace Directory dialog that fills workspace directory flows; for users and maintainers of the Web picking experience. |
| `@greeneek/gnk-client-ui-directory-picker-native` | Native directory-picker surface: the browser half that drives the host OS chooser for workspace-directory flows; for users and maintainers choosing a picking interaction. |
| `@greeneek/gnk-client-ui-goal` | Goal surface for the Web GUI: the composer-context strip that shows the current goal and edits, pauses, resumes, or clears it; for users and maintainers of the goal experience. |
| `@greeneek/gnk-client-ui-input-trigger` | Input trigger pipeline for the Web GUI: / and @ detection under the caret, the grouped candidate menu, and pick routing to registered sources; for users and maintainers of slash commands and references. |
| `@greeneek/gnk-client-ui-jobs` | Web background-job surface: the session-header action listing the jobs this session can see; for users and maintainers of the background-job experience. |
| `@greeneek/gnk-client-ui-layout` | Shell layout for the Web GUI: the three-column AppFrame with drag handles, concession behavior, the panel-geometry service, and theme presentation; for users and maintainers of the window chrome. |
| `@greeneek/gnk-client-ui-message-feedback` | Per-message feedback for the Web GUI: the Like/Dislike pair and optional note in the finalized assistant message's action row; for users and maintainers of the feedback experience. |
| `@greeneek/gnk-client-ui-model-selection` | Model selection for the Web GUI: the /model popup and the composer model seat over one per-session provider-grouped directory; for users and maintainers of model routing. |
| `@greeneek/gnk-client-ui-permission-presets` | Permission preset surfaces for the Web GUI: the General-settings default row and the /permission picker for the current session; for users and maintainers of permission policy. |
| `@greeneek/gnk-client-ui-plan` | Plan-mode status chip for the Web GUI: the composer control that shows plan mode is on and turns it off; for users and maintainers of plan mode. |
| `@greeneek/gnk-client-ui-primitives` | Shared React UI atoms for the gnk web client: controls, icons, markdown and math rendering, and the terminal/read/diff/search/web output cards (zero cordis). |
| `@greeneek/gnk-client-ui-reference` | Web @file and @session reference source for the composer: candidates, ordering, and atomic inline references (unified file/session picking). |
| `@greeneek/gnk-client-ui-renderer` | Browser UI renderer: React slot bindings, ctx.uiRenderer, and the assembled application root for the gnk web client. |
| `@greeneek/gnk-client-ui-schedule` | The read-only Web catalog for active Schedule reminders, for users choosing the surface and maintainers of its projection, timing, and accessibility behavior. |
| `@greeneek/gnk-client-ui-session` | React and Slot adapters for Session Controller lists, interaction state, and per-session context. |
| `@greeneek/gnk-client-ui-settings` | Settings domain base plugin: the settings-namespace scope service, schema service, and the canonical settings slot-type contract for the gnk web client. |
| `@greeneek/gnk-client-ui-settings-general` | Settings shell, ownerless copy, and durable product-onboarding namespace for the gnk web client: the General section, trigger chrome, and onboarding ledger projection. |
| `@greeneek/gnk-client-ui-settings-models` | Models settings and product-onboarding plugin for the gnk web client: provider rows, API-key management, model lists, and the Greeneek first-run dialogs. |
| `@greeneek/gnk-client-ui-settings-plugin-inventory` | Scope-grouped read-only plugin inventory tab in Web Plugins settings for the gnk web client: agent-preset compositions first, the global plane behind a disclosure, search across both. |
| `@greeneek/gnk-client-ui-settings-plugins` | Plugins settings section for the gnk web client: feature-owned tabs, the configurable host-plane plugin cards, and the settings.plugin.item extension point. |
| `@greeneek/gnk-client-ui-sidebar` | Sidebar shell plugin for the gnk web client: brand row, New Session action, collapse control, scroll-aware region seat, and bottom-pinned Settings seat. |
| `@greeneek/gnk-client-ui-skill` | Web skill references and the dedicated skill tool row for the gnk web client: the /-triggered skill source and the skill call card. |
| `@greeneek/gnk-client-ui-slots` | Slot registry pure core for the gnk web client: SlotMap declaration merging, the single register composition API, four-share props types, store seats, and the renderer install contract. |
| `@greeneek/gnk-client-ui-subagent` | Subagent conversation catalog, continuation routing UI, and '@' reference source for the gnk web client. |
| `@greeneek/gnk-client-ui-theme` | Theme and content-font-size settings for the gnk web client: --dsw-* token stylesheets, ThemeRuntime state, General settings rows, and the pre-plugin bootstrap. |
| `@greeneek/gnk-client-ui-tool` | Client Tool presentation plugin for the gnk web client: whole-call tree composition, the keyed per-tool view slot, and the built-in atomic tool cards. |
| `@greeneek/gnk-client-ui-trajectory` | Trajectory view for the gnk web client: a turn-aware event ledger with an interactive timing overview, registered into the conversation view ring. |
| `@greeneek/gnk-client-ui-user-questions` | Web ask_user_question feature for the gnk web client: the composer-takeover question UI and the plan-review approval card. |
| `@greeneek/gnk-client-ui-web-search` | Search-engine preference row for the gnk web client: engine choice plus custom endpoint in Settings General. |
| `@greeneek/gnk-client-ui-workflow-run` | Durable workflow-run Conversation Node for the gnk web client: reconstructs top-level workflow runs as independent chat nodes with nested member disclosure. |
| `@greeneek/gnk-client-ui-workspace` | Shared Workspace browser and picker plugin for the gnk web client: grouped or flat session rows, add/rename/reorder, search, fork, archive, and the directory-flow picking hole. |
| `@greeneek/gnk-client-ui-cordis` | Cordis dynamic-plugin browser surfaces for users and maintainers choosing, composing, or debugging the panel, tool cards, and @pluginId input. |

## LLM routes

| Package | Role |
| --- | --- |
| `@greeneek/gnk-greeneek-llm-api-extensions` | Official Greeneek request-extension registry for provider plugins contributing lifecycle-owned top-level API fields. |
| `@greeneek/gnk-llm` | The provider-neutral model-call service for users and maintainers streaming requests, registering provider adapters, or resolving model metadata. |
| `@greeneek/gnk-llm-greeneek` | The Greeneek chat-completions adapter for users and maintainers configuring the greeneek-official route, thinking, and image input. |
| `@greeneek/gnk-llm-pi-ai` | The pi-ai-backed multi-provider adapter for users and maintainers routing the harness LLM service through pi-ai catalogs and hand-declared gateways. |
| `@greeneek/gnk-llm-retry` | The retry executor for users and maintainers configuring provider-routed model-request recovery at durable agent-step boundaries. |
| `@greeneek/gnk-plugin-package-inventory-greeneek` | Active Loader package inventory metadata for deployments that attach package metadata to their own Greeneek-protocol requests. |
| `@greeneek/gnk-token-meter` | Replay-aware token and context-pressure measurement for users and maintainers sizing prompts or building compaction and occupancy displays. |

## Capabilities

### `acp/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-acp` | Automation-only Agent Client Protocol server for programmatic clients and maintainers driving Greeneek Harness agents over JSON-RPC stdio. |

### `api/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-api-gateway` | Typed Client-to-Host calls and streams: dispatch, validation, cancellation, reconnection, and forwarded Host events. |
| `@greeneek/gnk-api-remotes` | Application Remote assembly: selects typed Host capabilities and forwarded events for Client consumers. |
| `@greeneek/gnk-api-session-controller` | Host and Client session control: create, resume, prompt, follow history, and project live session state. |
| `@greeneek/gnk-api-settings-controller` | Host Remote owner for settings and credential configuration surfaces, including redacted reads, writes, credential references, and native document opening. |
| `@greeneek/gnk-api-workspace-controller` | Host and Client workspace control: mutate workspace navigation and follow its complete projection. |

### `attachment/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-attachment` | Durable image attachments for users and maintainers attaching, reusing, or debugging images in prompts and commands. |
| `@greeneek/gnk-attachment-local` | Local storage for your attached images below GNK_HOME, for users and maintainers choosing or debugging where image attachments are kept. |

### `boot/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-app-boot` | Shared Loader boot support for gnk profiles and the temporary Python SDK runtime: environment layers, patches, diagnostics, and configuration preview. |
| `@greeneek/gnk-cmdline` | App-owned command lines for gnk app bins: your app parses its own flags, --help, and exit behavior from the launcher's remaining arguments. |

### `bundle/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-acp-app` | Automation-only ACP stdio application profile for users and maintainers launching persistent harness agents. |
| `@greeneek/gnk-base` | The shared gnk core: model access, tools, durable sessions, and safety defaults for every gnk --profile surface, for users composing or customizing a profile. |
| `@greeneek/gnk-headless` | One-shot task mode for gnk: run a single task from the command line and get the final answer printed, for users scripting or automating gnk. |
| `@greeneek/gnk-sdk-app` | SDK stdio application profile for users and maintainers launching a JSON-RPC harness runtime. |
| `@greeneek/gnk-sdk-minimal` | Standalone two-tool SDK profile for users who need a minimal cross-platform coding agent without the shared base bundle. |
| `@greeneek/gnk-web-app` | The browser GUI for gnk: interactive chat, model and settings management, and session history, for users running the gnk web surface. |

### `client/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-client-connection` | Browser-host wire layer for the web GUI: Remote RPC, event-stream delivery with reconnect, exact Fetch routes, the /api HTTP bridge, and the browser-trust fence. |
| `@greeneek/gnk-client-hmr` | Development-only hot reload for browser client plugins: rebuilding a plugin bundle swaps the running plugin in place, for developers iterating on the web GUI. |
| `@greeneek/gnk-client-locale` | Localization for the web GUI: the zh/en preference, browser-derived fallback, typed namespace dictionaries, and the framework translation seat, for users and plugin authors. |
| `@greeneek/gnk-client-modules` | Client module system for the web GUI: the host composes the boot graph and serves plugin bundles, and the browser loads them lazily, for users and maintainers composing or debugging client plugins. |
| `@greeneek/gnk-client-store` | Observable browser state stores with explicit snapshots, subscriptions, and lifecycle ownership. |
| `@greeneek/gnk-client-web` | Web boot kernel for the web GUI: two-stage boot of the client plugin tree, the framework-free boot page, and the shared module table, for users and maintainers composing or debugging the browser application. |

### `code-runtime/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-code-runtime` | Abstract code-execution seam (`ctx.codeRuntime`) for users and maintainers composing, consuming, or building a backend that runs one model-written program against host-provided bindings. |
| `@greeneek/gnk-code-runtime-worker-thread` | Worker-thread code execution for users and maintainers composing, sizing, or debugging the shipped TypeScript backend that runs each program in a fresh Node worker. |

### `compaction/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-command-compact` | The on-demand /compact command for interactive compositions: what it does, what you see, and how to mount it. |
| `@greeneek/gnk-compaction` | Shared compaction contract for backend implementers and deployers: what conversation condensation does, when to use it, and how to build a backend. |
| `@greeneek/gnk-compaction-basic` | Automatic conversation condensation for deployments choosing, tuning, or debugging how older history is summarized as token pressure builds. |
| `@greeneek/gnk-compaction-tool-result-pruner` | Tool-output trimming for deployments composing compaction: choosing size limits or debugging why oversized tool results get shortened. |

### `context/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-agent-instructions` | Workspace-instruction context for users and maintainers enabling, sizing, or debugging AGENTS.md/CLAUDE.md loading and refresh. |
| `@greeneek/gnk-file-reference` | File-reference discovery and @file mention grammar for host-backed UIs, for users and maintainers choosing the seam or pairing it with a provider. |
| `@greeneek/gnk-file-reference-local` | Local-workspace @file completion provider for users and maintainers enabling, sizing, or debugging ctx.fileReferences discovery. |
| `@greeneek/gnk-session-reference` | Cross-session snapshot references and durable untrusted model context, for users and maintainers enabling or debugging ctx.sessionReferenceResolver. |
| `@greeneek/gnk-time-context` | Opt-in per-step clock context with the current time, browser zone, and elapsed time, for users and maintainers enabling or tuning the plugin. |
| `@greeneek/gnk-tmux-context` | Opt-in per-turn tmux location context for users and maintainers enabling or tuning the agent's session, window, and pane awareness. |

### `core/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-agent` | The Agent handle, live registry, process-local initiator scope, and agent/* event vocabulary for plugins, UI, and orchestrators building or extending agents. |
| `@greeneek/gnk-agent-default-model` | The deployment default model selection for users and maintainers choosing, configuring, or debugging which model freshly created agents start on. |
| `@greeneek/gnk-agent-loop` | The default agent driver for users and maintainers choosing, configuring, or debugging how agents are created and how turns and steps run. |
| `@greeneek/gnk-agent-tool-presentation` | The agent-plane presentation selector for users and maintainers choosing, configuring, or debugging which form of its tools an agent preset's models see. |
| `@greeneek/gnk-scope` | The scoped-registration library for plugin authors and maintainers building registries or event surfaces that isolate contributions per agent or per group. |
| `@greeneek/gnk-session` | The event-sourced session log and in-memory store for users and maintainers building, inspecting, or extending the durable record behind every agent interaction. |
| `@greeneek/gnk-system-prompt` | System-prompt assembly for users and maintainers adding prompt sections, variables, tool-schema sources, or configuring the model-facing prompt. |
| `@greeneek/gnk-tools` | The tool registry and execution pipeline for tool authors and maintainers registering, restricting, presenting, or debugging model-facing tools. |

### `credentials/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-authorization` | The authorization flow registry for users and maintainers who obtain credentials that configuration cannot supply, because getting one means a conversation with a human. |
| `@greeneek/gnk-credentials` | The credential seam for users and maintainers resolving, describing, or storing credentials — reference values and durable records — without putting secret values in configuration. |
| `@greeneek/gnk-credentials-local` | The file-backed credentials provider for users and maintainers choosing, configuring, or debugging the local credential store and its environment layering. |

### `e2b/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-e2b` | One shared remote Linux sandbox for E2B-backed file and command work: configuration, lifetime, and what happens at startup and shutdown. |
| `@greeneek/gnk-fs-e2b` | File operations inside the shared remote sandbox: what the agent can do with files there, when to use it, and what to expect — for deployments and maintainers of the E2B family. |
| `@greeneek/gnk-subprocess-e2b` | Shell commands and terminals inside the shared remote sandbox: what the agent can run there, how output is handled, and what to expect — for deployments and maintainers of the E2B family. |

### `experimental/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-experimental-agent-team` | Run a small team of named agents in one session: durable messages between members and a shared task board, for deployments composing the experimental Team plugins. |
| `@greeneek/gnk-experimental-agent-team-profile` | Private Agent Teams profile layer over gnk-base, for source-checkout users who want Team-scoped coordination tools while retaining one-shot delegation. |
| `@greeneek/gnk-experimental-agent-team-web-profile` | Add the experimental Agent Teams panel to a source-checkout Web profile after the Host Team layer. |
| `@greeneek/gnk-experimental-client-ui-agent-team` | Use and debug the experimental Web Agent Teams roster, shared task board, and teammate navigation panel. |
| `@greeneek/gnk-experimental-code-runtime-python` | CPython-subprocess code runtime: the gnk-code-runtime seam implementation for Python model code, with the fd-3 wire protocol it speaks. |
| `@greeneek/gnk-experimental-inspector` | Experimental Chrome DevTools inspection for Host and browser Client Cordis runtimes, including Console evaluation, Sources, Network capture, Elements trees, and a CDP-independent query API. |
| `@greeneek/gnk-experimental-webworker-packer` | Browser-worker VFS image packaging for maintainers building or debugging the experimental preview deployment. |
| `@greeneek/gnk-experimental-webworker-runtime` | Browser-worker harness hosting for maintainers building or debugging the experimental Web preview runtime. |

### `extensions/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-cordis-client-runner` | Browser half of dynamic Cordis packages for users and maintainers choosing, composing, or debugging how a page answers run requests and loads browser-half code. |
| `@greeneek/gnk-cordis-host-runner` | Host half of dynamic Cordis packages for agents and maintainers choosing, composing, or debugging the registry, sandbox, and run round trip. |

### `feedback/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-command-feedback` | Free-text session feedback through a `/feedback` command, for users and maintainers choosing, composing, or debugging feedback capture. |
| `@greeneek/gnk-message-feedback` | Per-message ratings and notes for finalized assistant messages, for users and maintainers choosing, composing, or debugging the feedback service. |

### `fs/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-fs` | The ctx.fs filesystem service contract for deployments choosing or mounting a filesystem backend and developers implementing one. |
| `@greeneek/gnk-fs-local` | The host-filesystem backend for ctx.fs for deployments and maintainers choosing or debugging local file access. |
| `@greeneek/gnk-fs-observation-policy` | The read-before-edit filesystem policy plugin for deployments and maintainers choosing or debugging guarded write and edit behavior. |
| `@greeneek/gnk-fs-sandbox` | The sandbox-enforcing ctx.fs backend for deployments and maintainers confining model file mutations to a session workspace. |

### `goal/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-command-goal` | The human-facing /goal slash command for users and maintainers choosing, composing, or debugging goal control in UI command planes. |
| `@greeneek/gnk-goal` | The persisted same-session goal service for users and maintainers choosing, configuring, or debugging one durable completion objective per session. |
| `@greeneek/gnk-goal-round-driver` | The same-session continuation driver for users and maintainers choosing, composing, or debugging automatic goal rounds. |

### `guard/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-repeat-tool-reminder` | Advisory loop-hygiene guard that nudges the model out of identical tool-call loops, for users and maintainers choosing, configuring, or debugging the plugin. |
| `@greeneek/gnk-tool-call-timeout-policy` | Cooperative time limit for cancellation-aware tool calls, mapping a settled timeout to a clear model error for users and maintainers choosing or debugging the plugin. |

### `hooks/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-hook-protocol` | The shared hook rules behind the Claude Code and Codex bridges — what a hook can do and what happens when it runs — for users and maintainers of the hooks subsystem. |
| `@greeneek/gnk-hooks-claude-code` | Run your existing Claude Code hooks.json or settings hook config during agent runs — block prompts and tools, attach context, or force continuation — for users and maintainers of the bridge. |
| `@greeneek/gnk-hooks-codex` | Run your existing Codex hooks.json hook config during agent runs — block prompts and tools, attach context, or force continuation — for users and maintainers of the bridge. |

### `host/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-host-directory-picker` | Workspace-directory picking seam for the web GUI host: the service contract, capability vocabulary, and error codes the native and browse backends implement. |
| `@greeneek/gnk-host-directory-picker-auto` | Adaptive chooser of the directory-picker seam: resolves the web GUI host's situation once at boot and mounts the matching native or browse backend. |
| `@greeneek/gnk-host-directory-picker-browse` | In-app browsing backend of the directory-picker seam: one-level directory listing and child-directory creation for the web GUI host, serving remote clients too. |
| `@greeneek/gnk-host-directory-picker-native` | Native-OS-chooser backend of the directory-picker seam: opens one platform chooser per pick for operators sitting at the web GUI host's display. |
| `@greeneek/gnk-host-frontend-static` | SPA dist server for the Web shell: claims the webserver fallback seat and serves the built frontend with traversal rejection and SPA index fallback. |
| `@greeneek/gnk-host-plugin-inventory` | Read-only projection of the current Cordis Loader plugin state with each agent preset's composition beside it: the pluginInventory service and its pluginInventory/list Remote for web GUI host clients. |
| `@greeneek/gnk-host-webserver` | The web GUI host's HTTP server: named-route and upgrade registration, index transforms, and the single fallback seat that serves the Web shell's SPA dist. |

### `identity/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-anonymous-user-id` | Anonymous per-harness-home identity for users and maintainers tracing how telemetry, feedback acknowledgement, and Greeneek provider requests correlate records. |

### `interaction/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-commands` | Human slash-command registry for interactive UIs: plugin-owned commands that run directly against an agent without creating a model message, for users and maintainers composing or extending command surfaces. |
| `@greeneek/gnk-permission-presets` | User-facing permission presets for users and maintainers choosing, configuring, or debugging the Permissions selector that bundles sandbox mode with an approval policy. |
| `@greeneek/gnk-user-approval` | Channel-neutral one-shot approval seam for users and maintainers composing answerers, setting policy, or debugging fail-closed permission decisions. |
| `@greeneek/gnk-user-questions` | Waterfall-based question and answer service for tools, permission plugins, local answerers, and Agent-scoped Web interactions. |

### `jobs/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-jobs` | The background-job registry contract for users and maintainers composing, implementing, or debugging background work: ids, ownership, lifecycle, and completion listeners. |
| `@greeneek/gnk-jobs-local` | The process-local background-job registry for users and maintainers composing, sizing, or debugging in-process jobs: per-owner admission, lifecycle, and teardown. |

### `lsp/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-lsp` | The LSP capability seam (ctx.lsp): provider selection by file extension, four normalized code-navigation operations, and structured errors, for users and maintainers composing or extending code navigation. |
| `@greeneek/gnk-lsp-stdio` | The stdio language-server provider for ctx.lsp: configured server commands, extension mappings, and bounded transient-open queries, for users and maintainers composing local code navigation. |

### `mcp/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-mcp-client` | MCP client bridge for deployments and maintainers choosing, configuring, or debugging connections to external MCP servers whose tools register on ctx.tools. |

### `plan/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-plan-mode` | Plan mode for users and maintainers choosing, configuring, or debugging the per-agent planning feature with deployment guidance, a /plan command, and a user-reviewed exit. |

### `preset/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-agent-presets` | Per-session agent composition from preset cordis.yml files, for users and maintainers choosing, configuring, or debugging agent presets. |
| `@greeneek/gnk-persona` | The composable persona row presets mount to give one agent its own system-prompt persona, for users and maintainers configuring or debugging it. |

### `runtime-diagnostics/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-invariants` | Runtime invariant checks for live compositions: the registry service that runs package-owned checks, for users and maintainers choosing, configuring, or debugging them. |

### `sandbox/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-sandbox` | The process-sandbox service contract for users and maintainers composing, using, or extending same-world subprocess confinement. |
| `@greeneek/gnk-sandbox-local` | Local per-platform sandbox backends for users and maintainers choosing, configuring, or debugging process confinement on Linux, macOS, or Windows. |
| `@greeneek/gnk-sandbox-policy` | The shared per-call sandbox policy resolver and current model context for users and maintainers composing, configuring, or debugging file-effect policy across enforcing capabilities. |
| `@greeneek/gnk-sandbox-windows-acl` | The Windows write-restriction sandbox backend for users and maintainers choosing, configuring, or debugging restricted-token process confinement on Windows. |

### `schedule/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-schedule` | Session-local durable reminders: the schedule_create, schedule_list, and schedule_delete tools and live-owner delivery, for users and maintainers choosing, configuring, or debugging the package. |

### `sdk/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-sdk-client` | The TypeScript SDK client for callers that spawn a Greeneek Harness runtime subprocess and drive agent turns over stdio JSON-RPC: the GreeneekHarness run API and the lower-level HarnessClient. |
| `@greeneek/gnk-sdk-protocol` | The SDK wire protocol for client and server implementers: the newline-delimited JSON-RPC transport and the named request, result, and notification types spoken between a Harness runtime and its SDK clients. |
| `@greeneek/gnk-sdk-jsonrpc-server` | The stdio JSON-RPC serving plugin for deployments that let out-of-process SDK clients open sessions and drive agents in a Greeneek Harness runtime. |

### `session/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-session-checkpoint-policy` | Semantic session durability checkpoints for users and maintainers deploying persisted agents that must not lose a model request or tool side effect on crash. |
| `@greeneek/gnk-session-log-greeneek` | Incremental canonical session-log upload for deployments that attach session metadata to their own Greeneek-protocol requests. |
| `@greeneek/gnk-session-persistence` | The durable session-storage seam for users and maintainers choosing a persistence backend, resuming sessions, or building a backend against the shared service contract. |
| `@greeneek/gnk-session-persistence-jsonl` | The shipped JSONL session-persistence backend for deployments and maintainers choosing, configuring, or debugging per-session durable logs with optional Zstandard compression. |
| `@greeneek/gnk-session-projection` | The session-projection registry for developers serving whole current values of log-derived per-session state to client carriers, and for maintainers of the drive contract. |
| `@greeneek/gnk-session-projection-cache` | The persisted session-projection cache for deployments and maintainers choosing, configuring, or debugging durable checkpoints, zero-I/O list reads, and accelerated cold projection folds. |
| `@greeneek/gnk-session-stats` | Whole-log conversation counts and wall times for clients and maintainers choosing, composing, or debugging the sessionStats projection unit. |
| `@greeneek/gnk-session-telemetry` | Session-telemetry capture seam for deployments and backend authors choosing a reporting backend, mounting redaction rules, or implementing the backend contract. |
| `@greeneek/gnk-session-telemetry-otel` | OpenTelemetry session-telemetry backend for deployments choosing a mode, configuring the exporter, or tracing what leaves the machine. |
| `@greeneek/gnk-session-title` | Log-backed session titles for users and maintainers choosing a title source, configuring the service, or debugging title state. |
| `@greeneek/gnk-session-title-all-prompts-llm` | All-messages LLM session-title provider for users and maintainers choosing a title strategy or debugging automatic title generation. |
| `@greeneek/gnk-session-title-first-prompt-llm` | First-message LLM session-title provider for users and maintainers choosing a title strategy or debugging automatic title generation. |
| `@greeneek/gnk-session-title-llm` | Shared model-backed title generation policy for users and maintainers configuring title providers or debugging auxiliary LLM requests. |
| `@greeneek/gnk-session-turn-outline` | Whole-log turn outline for clients and maintainers composing or debugging the turnOutline projection unit behind full-session turn navigation. |

### `session-query/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-session-log-export` | Web Session-log ZIP export: Host streaming, the authenticated download route, the Session Header action, and the /export command. |
| `@greeneek/gnk-session-query` | The unified session-history query service for consumers and backend authors: exact reads, relationship traces, and provider-independent filters over live and durable session logs. |
| `@greeneek/gnk-session-query-sqlite` | The SQLite FTS5 full-text search backend for session history, for deployments and maintainers choosing, configuring, or debugging full-text search over the query service. |

### `settings/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-settings` | The user-settings service for plugin authors and maintainers registering configurable namespaces, reading resolved values, or wiring configuration surfaces. |
| `@greeneek/gnk-settings-file` | The file-backed settings provider for users and maintainers choosing, configuring, or debugging the YAML/JSON settings document and its hot reload. |

### `shell/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-bash-local` | The default POSIX Bash executor for deployments and maintainers choosing, configuring, or debugging unconfined command execution over the shell seam. |
| `@greeneek/gnk-bash-sandbox` | The sandbox-consuming Bash executor for deployments and maintainers choosing, configuring, or debugging confined command execution with denial and escalation facts. |
| `@greeneek/gnk-pwsh-local` | The local PowerShell executor for deployments and maintainers choosing, configuring, or debugging unconfined PowerShell command execution over the shell seam. |
| `@greeneek/gnk-pwsh-sandbox` | The sandbox-consuming PowerShell executor for deployments and maintainers choosing, configuring, or debugging confined PowerShell command execution with denial facts. |
| `@greeneek/gnk-shell` | The bash executor seam for developers and maintainers choosing, composing, or implementing command execution over ctx.shell. |
| `@greeneek/gnk-shell-env` | The managed GNK_* shell environment for users and maintainers choosing, configuring, or extending the environment every model shell call runs with. |

### `spill/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-spill` | The spill storage service: how deployments and plugin authors save oversized tool text and get back a retrievable locator. |
| `@greeneek/gnk-spill-local` | The local filesystem spill backend: how spilled tool output is saved to private session-scoped files and retrieved with read or grep. |
| `@greeneek/gnk-spill-policy` | The tool-result spill policy: how deployments keep oversized plain-text tool results out of the model's context with a preview and a retrievable spill file. |

### `storage/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-storage` | Storage hub (ctx.storage) for compositions and maintainers choosing, mounting, or debugging named storage backends and data-form facilities. |
| `@greeneek/gnk-storage-domain` | Domain data form (ctx.storageDomain) for hosts and maintainers choosing, mounting, or debugging schema-validated, change-emitting KV domains over storage backends. |
| `@greeneek/gnk-storage-json` | JSON storage backend for hosts and maintainers choosing, configuring, or debugging whole-unit and per-record files under a configured root. |
| `@greeneek/gnk-storage-sqlite` | SQLite storage backend for hosts and maintainers choosing, configuring, or debugging document-per-row KV storage in one database file. |

### `subagent/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-subagent` | The subagent delegation seam for users and maintainers choosing a provider backend, composing delegation tools, or debugging child-agent runs. |
| `@greeneek/gnk-subagent-acp` | The out-of-process ACP subagent backend for users and maintainers choosing a delegation provider, configuring a child ACP agent command, or debugging remote child runs. |
| `@greeneek/gnk-subagent-claude-code` | The one-shot Claude Code subagent provider for users and maintainers choosing a product backend, installing a Profile bundle, or configuring an unattended Claude Code delegation. |
| `@greeneek/gnk-subagent-codex` | The one-shot Codex subagent provider for users and maintainers choosing a product backend, installing a Profile bundle, or configuring an unattended Codex delegation. |
| `@greeneek/gnk-subagent-fork-in-process` | In-process fork subagent backend for users and maintainers choosing, configuring, or debugging children seeded with the parent's completed turns. |
| `@greeneek/gnk-subagent-gnk-sdk` | The out-of-process SDK subagent backend for users and maintainers choosing a delegation provider, configuring a child Harness runtime command, or debugging remote child runs. |
| `@greeneek/gnk-subagent-in-process-driver` | Shared in-process subagent run driver for maintainers and backend authors understanding or extending the spawn and fork run lifecycle. |
| `@greeneek/gnk-subagent-spawn-in-process` | In-process spawn subagent backend for users and maintainers choosing, configuring, or debugging fresh-child delegation. |

### `subprocess/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-subprocess` | The subprocess service (ctx.subprocess) for composition authors and capability consumers starting, observing, and terminating managed child processes and terminal sessions. |
| `@greeneek/gnk-subprocess-local` | The local host provider for the subprocess service: run managed process trees and real terminal sessions on the host machine. |
| `@greeneek/gnk-win32-process` | Low-level Win32 process primitives for maintainers implementing or debugging the Windows ACL sandbox. |

### `terminal/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-terminal` | Persistent terminal sessions for deployments and consumers choosing, composing, or extending the owner-scoped ctx.terminals service. |
| `@greeneek/gnk-terminal-bash` | The shipped shell backend for persistent terminal sessions: interactive bash or pwsh under the shared sandbox policy, with readiness detection and bounded line-oriented output. |

### `test-support/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-agent-loop-testkit` | Shared service mounting for tests that exercise the concrete AgentLoop, for test authors wiring real loop prerequisites. |
| `@greeneek/gnk-client-test-runtime` | jsdom slot test runtime for browser feature specs, for test authors exercising slots, stores, and rendering against production machinery. |
| `@greeneek/gnk-llm-mock-server` | Scriptable OpenAI-compatible fault server for testing LLM adapters and recovery policy without a provider key, for test authors and demos. |
| `@greeneek/gnk-llm-replay` | Keyless LLM replay plugin for snapshot tests, for test authors booting the real agent against recorded model transcripts. |
| `@greeneek/gnk-loader-smoke` | Shared subprocess and direct-agent harness for keyless example smoke tests, for test authors booting real Loader compositions. |
| `@greeneek/gnk-session-snapshot` | Session-log snapshot support for keyless profile tests: manifests, identity redaction, normalization, workspace checks, and protocol adapters. |

### `typert/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-typert-generator` | The build-time Typert generator: source type analysis, compiler-independent models, and artifact emission for maintainers wiring Typert publication or consuming generated artifacts. |
| `@greeneek/gnk-typert-loader` | Loader integration for generated Typert artifacts: how mounted packages automatically contribute their host-face reflection and schemas to the runtime registry. |
| `@greeneek/gnk-typert-protocol` | The shared Typert Remote protocol: decorators, wire descriptors, codecs, and provider contracts used by business packages, generated artifacts, the Host Gateway, and the Client API. |
| `@greeneek/gnk-typert-registry` | The runtime Typert registry: stores generated package reflection, live Zod schemas, and Remote invocation descriptors, and resolves them for consumers. |

### `util/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-atomic-write` | Atomic file replacement and cross-process writer locking for packages that must never leave partial, symlink-hijacked, or wider-permission content on disk. |
| `@greeneek/gnk-brand` | Nominal string and number types with stateless constructors for packages that own confusable domain values. |
| `@greeneek/gnk-util-crypto` | Cross-runtime UUID generation for maintainers replacing secure-context-only crypto.randomUUID calls. |
| `@greeneek/gnk-deque` | Circular deque for Host and browser packages that need amortized constant-time queue operations, immediate release of removed entries, and bounded vacant storage. |
| `@greeneek/gnk-egress` | The single network egress policy of the Greeneek Harness: no provider is blocked by default, plus an optional strict allow-list for air-gapped deployments. |
| `@greeneek/gnk-home-paths` | Shared resolution of the Greeneek Harness home and user-data paths for packages that need one consistent root, tilde expansion, and stable watch paths. |
| `@greeneek/gnk-launch-environment` | An immutable snapshot of this run's environment that remembers which layer supplied each value, for packages that must resolve user-facing values without trusting a flattened process.env. |
| `@greeneek/gnk-native-command` | Host-native command and path-opening utilities with shell-free execution, cancellation, desktop detection, and WSL path handoff. |
| `@greeneek/gnk-output-retention` | Bounded model-facing output for tools that must cap how much context they return: item and text retainers plus a standardized omission footer. |
| `@greeneek/gnk-util-time` | IANA time-zone validation and canonicalization for maintainers accepting a caller-reported zone at a wire boundary. |
| `@greeneek/gnk-timeout` | Shared timeout arithmetic, deadline fusion, and timeout-versus-cancel classification for capabilities that clamp a caller's hint, arm a deadline, and must tell the two apart later. |
| `@greeneek/gnk-util-values` | Lossless JSON validation, detached snapshots, deep freezing, structural equality, and exhaustive-union helpers for runtime packages. |
| `@greeneek/gnk-util-workspace-path` | Browser-safe Workspace path helpers for joining relative paths, abbreviating POSIX homes, and deriving display titles. |

### `web/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-web` | The web access service (ctx.web): how deployments and plugin authors search the web and fetch URLs through interchangeable providers, with one selection policy and error vocabulary. |
| `@greeneek/gnk-web-fetch-http` | The anonymous public HTTP(S) fetch backend for ctx.web: how deployments mount bounded, safe URL retrieval with same-origin redirects and text-only decoding. |
| `@greeneek/gnk-web-search-duckduckgo` | The DuckDuckGo-backed search provider for ctx.web: keyless web search through Instant Answer with portable sources. |
| `@greeneek/gnk-web-search-exa` | The Exa-backed search provider for ctx.web: how deployments mount vendor-native web search with portable snippets and publication dates. |
| `@greeneek/gnk-web-search-google` | The Google-backed search provider for ctx.web: how deployments mount vendor-native web search through Programmable Search with portable snippets. |
| `@greeneek/gnk-web-search-greeneek` | A Greeneek-protocol search provider for ctx.web: how deployments mount native-style web search through an Anthropic-compatible Messages API endpoint they operate, with per-search credential resolution. |
| `@greeneek/gnk-web-search-perplexity` | The Perplexity-backed search provider for ctx.web: how deployments mount OpenAI-compatible Perplexity search with generated answers and citations. |

### `webhook/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-webhook` | Webhook rule runtime for maintainers registering trusted external-event policies that create Workspace Sessions. |
| `@greeneek/gnk-webhook-github` | Signed GitHub webhook adapter for deployments routing authenticated JSON events into the webhook runtime. |

### `workflow/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-workflow` | The workflow orchestration capability: run a model-written script that fans out subagents, for users and maintainers choosing or building on ctx.workflowEngine. |
| `@greeneek/gnk-workflow-worker-thread` | The worker-thread workflow engine: executes model-written orchestration scripts off the host event loop, for users and maintainers choosing or configuring execution isolation. |

### `workspace/`

| Package | Role |
| --- | --- |
| `@greeneek/gnk-workspace` | Workspace entity registry (ctx.workspaceRegistry) for hosts choosing, mounting, or debugging durable workspace records and header-validated session membership. |
