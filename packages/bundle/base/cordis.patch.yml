# The gnk-base bundle patch: the shared core of each base-backed profile, applied as
# ONE insert over the empty profile root. Later bundle patches and the user's
# profile cordis.patch.yml address these rows by id, with the last write
# winning per row.
#
# A patch replaces the targeted row's whole `config` rather than merging into
# it, so a row whose value differs by mode does NOT live here: it belongs to
# each mode bundle, keeping any single row down to one bundle layer plus the
# user's. Mode-specific rows appear below only with shared plugin identity and
# neutral defaults; each mode bundle restates its complete configuration.
#
# Row order carries no load semantics (activation is service-availability
# driven); the grouping is for readers.

- insert:
    - id: timer
      name: '@greeneek/cordis-plugin-timer'

    # Module reload is opt-in per profile. `patchReload: live` config watching
    # uses the launcher's watch-only fallback and does not require this row.
    - id: hmr
      name: '@greeneek/cordis-plugin-hmr'
      disabled: true
      config:
        root: ['.']

    - id: llm
      name: '@greeneek/gnk-llm'

    - id: greeneek-llm-api-extensions
      name: '@greeneek/gnk-greeneek-llm-api-extensions'

    - id: session
      name: '@greeneek/gnk-session'

    # The session-log upload is NOT mounted here. It contributes request
    # fields only to the official Greeneek API, which no deployment of this
    # repository operates; shipping it would attach inert bytes to every
    # request. A deployment that runs its own Greeneek-protocol endpoint
    # mounts it itself:
    #
    #   - insert:
    #       - id: session-log-greeneek
    #         name: '@greeneek/gnk-session-log-greeneek'

    - id: typert
      name: '@greeneek/gnk-typert-registry'

    - id: typert-loader
      name: '@greeneek/gnk-typert-loader'

    - id: typert-gateway
      name: '@greeneek/gnk-api-gateway'

    - id: session-title
      name: '@greeneek/gnk-session-title'
      config:
        fallbackMaxWords: 5
        fallbackMaxBytes: 40
        maxTitleBytes: 80

    - id: session-title-llm
      name: '@greeneek/gnk-session-title-first-prompt-llm'
      config:
        targetWords: 5
        targetCjkCharacters: 10
        maxInputBytes: 4096
        maxOutputTokens: 64
        timeoutMs: 60000

    - id: user-questions
      name: '@greeneek/gnk-user-questions'

    - id: agent
      name: '@greeneek/gnk-agent'

    # The package-inventory contribution is NOT mounted here. It reports to
    # the official Greeneek API, which no deployment of this repository
    # operates; shipping it would attach inert bytes to every request.
    # A deployment that runs its own Greeneek-protocol endpoint mounts it
    # itself:
    #
    #   - insert:
    #       - id: plugin-package-inventory-greeneek
    #         name: '@greeneek/gnk-plugin-package-inventory-greeneek'

    # The transport-independent default for Agents created by entry points.
    # Settings may supply a saved selection; consumers read it at creation time.
    # No provider/model is pinned: this build ships no provider of its own, so
    # the default is whichever route the user's own API key activated (the
    # first registered one until they choose otherwise) and the model picker's
    # saved choice from then on. A deployment that wants a fixed default states
    # `provider` and `model` here.
    - id: agent-default-model
      name: '@greeneek/gnk-agent-default-model'

    - id: jobs
      name: '@greeneek/gnk-jobs-local'

    - id: llm-retry
      name: '@greeneek/gnk-llm-retry'

    # User-settings document (`$GNK_HOME/settings.yaml`, hot-reloaded): an
    # `llm-pi-ai:` section there activates provider routes with the user's own
    # keys without a restart, and is what the web Models page writes.
    - id: settings
      name: '@greeneek/gnk-settings-file'

    # Credential sources: inherited environment over the managed
    # `$GNK_HOME/.credentials.yaml`, with project and user `.env` fallbacks.
    # Adapters resolve references per request; the Models page writes only the
    # managed document, which is never materialized into the process environment.
    - id: credentials
      name: '@greeneek/gnk-credentials-local'

    # The pi-ai multi-provider twin, mounted dormant: zero routes (and no extra
    # models in the picker) until a `llm-pi-ai:` settings section supplies provider
    # profiles — then those routes register live, keys resolving per request
    # through their apiKeyEnv references, and drop again when the section empties.
    # Supplying those profiles is exactly what the web Models page does. Which
    # adapters exist is composition; which providers run is the user's settings
    # document.
    - id: llm-pi-ai
      name: '@greeneek/gnk-llm-pi-ai'

    - id: session-persistence-jsonl
      name: '@greeneek/gnk-session-persistence-jsonl'
      config:
        root: !!js gnkHomePath('sessions')

    # Durable image bytes live outside the append-only session log. Messages
    # keep content-addressed references that this shared backend resolves for
    # provider requests and authorized history reads.
    - id: attachment-local
      name: '@greeneek/gnk-attachment-local'

    # Full-text session search is opt-in. `openAt: never` keeps
    # ctx.sessionQuery mounted — exact reads, titles, and lineage traces
    # (session export, subagent-fork Workspace inheritance) stay available —
    # while search calls fail with SESSION_QUERY_SEARCH_DISABLED and SQLite is
    # never opened; the Web sidebar search matches titles and workspace names
    # only. Deployments enabling content search override `openAt` to
    # `first-search` or `startup` in a later patch layer (profile
    # cordis.patch.yml or a --patch overlay), typically with a durable `path`.
    - id: session-query-sqlite
      name: '@greeneek/gnk-session-query-sqlite'
      config:
        path: ':memory:'
        openAt: never

    # Shared projection registry: subagent catalog identity (mode/label) folds
    # through its registered units, so the `list_agents` surface below fails
    # loud without it; web layers reuse this same mount for list rows.
    - id: session-projection
      name: '@greeneek/gnk-session-projection'

    # Durable KV storage: the storage hub, the json backend, and the
    # schema-validated domain form over them. Session-layer persistence (the
    # projection cache below; workspace and message-feedback in web layers)
    # routes through this stack, so it belongs to the shared base.
    - id: storage
      name: '@greeneek/gnk-storage'

    - id: storage-json
      name: '@greeneek/gnk-storage-json'
      config:
        root: !!js gnkHomePath('storages')

    - id: storage-domain
      name: '@greeneek/gnk-storage-domain'
      config:
        backend: json

    # Persisted projection cache: throttled write-behind over the
    # session_projcache domain (per-record layout — one version-stamped
    # checkpoint document per session), serving the session listing's
    # projection column.
    - id: session-projection-cache
      name: '@greeneek/gnk-session-projection-cache'
      config:
        writeEveryEvents: 200
        writeIntervalMs: 5000

    # Session telemetry defaults to feedback-gated sharing: FEEDBACK_ONLY
    # uploads only when the user records /feedback, releasing the session
    # records since the last handoff through that event (a resumed session
    # shares only its current lifecycle). GNK_TELEMETRY_MODE overrides to
    # FULL or DISABLED; uploading mirrors session-log records onto OTLP/HTTP
    # logs with no session-telemetry/record redaction rule, so exports are
    # the raw captured copy. The deployment stance, env seams, and follow-ups
    # are pinned in the feedback-gated-default Agent Note.
    # GNK_TELEMETRY_OTLP_URL overrides the production endpoint. A non-empty
    # GNK_TELEMETRY_DISABLED — any value, including '0'/'false' — opts the
    # process out (the launchers patch the row disabled; config cannot disable
    # a row). Exports carry the harness home's anonymous user id ($GNK_HOME/.anonymous-user-id,
    # random UUID; delete the file to reset the identity) as the Resource's
    # user.id. The exporter/processor values normally bound the shutdown drain
    # to ~1s against an unreachable collector: exporter.timeoutMillis is both
    # the per-attempt socket timeout and the retry deadline (1s effectively
    # disables the SDK's 5-try backoff), while maxExportBatchSize == maxQueueSize
    # (both explicit) makes the drain a single batch. The SDK awaits
    # exporter.forceFlush() outside exportTimeoutMillis, so the backend's 3s
    # shutdownTimeoutMillis is the load-bearing outer bound when a transport
    # promise never settles. Every CLI exit path drains it by disposing the root
    # on SIGINT/SIGTERM.
    - id: session-telemetry-otel
      name: '@greeneek/gnk-session-telemetry-otel'
      config:
        mode: !!js process.env.GNK_TELEMETRY_MODE || 'FEEDBACK_ONLY'
        shutdownTimeoutMillis: 3000
        exporter:
          url: !!js process.env.GNK_TELEMETRY_OTLP_URL ?? 'https://harness-telemetry.greeneeksvc.com/v1/logs'
          compression: gzip
          timeoutMillis: 1000
        processor:
          scheduledDelayMillis: 10000
          maxQueueSize: 2048
          maxExportBatchSize: 2048
          exportTimeoutMillis: 1500

    - id: subprocess
      name: '@greeneek/gnk-subprocess-local'

    # Every shipped CLI mode starts with the same file-effect boundary.
    # The environment remains an explicit deployment override; otherwise fresh
    # sessions pin workspace-write + ask through the permission service below.
    - id: sandbox
      name: '@greeneek/gnk-sandbox-local'

    - id: sandbox-policy
      name: '@greeneek/gnk-sandbox-policy'
      config:
        mode: !!js process.env.GNK_PERMISSION_MODE ?? 'workspace-write'
        workspaceRoot: !!js process.cwd()

    - id: bash-sandbox
      name: '@greeneek/gnk-bash-sandbox'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 60000

    - id: pwsh-sandbox
      name: '@greeneek/gnk-pwsh-sandbox'
      disabled: !!js process.platform !== 'win32'

    - id: approval
      name: '@greeneek/gnk-user-approval'
      config:
        policy: !!js "(process.env.GNK_PERMISSION_MODE ?? 'workspace-write') === 'danger-full-access' ? 'never' : 'ask'"

    - id: permission
      name: '@greeneek/gnk-permission-presets'
      config:
        presets:
          read-only:
            sandbox: read-only
            approval: ask
          workspace-write:
            sandbox: workspace-write
            approval: ask
          danger-full-access:
            sandbox: danger-full-access
            approval: never

    - id: shell-env
      name: '@greeneek/gnk-shell-env'

    - id: tool-bash
      name: '@greeneek/gnk-tool-bash'
      disabled: !!js process.platform === 'win32'

    - id: tool-pwsh
      name: '@greeneek/gnk-tool-pwsh'
      disabled: !!js process.platform !== 'win32'

    - id: tool-jobs
      name: '@greeneek/gnk-tool-jobs'

    - id: fs-observation-policy
      name: '@greeneek/gnk-fs-observation-policy'

    - id: tool-fs
      name: '@greeneek/gnk-tool-fs'

    - id: tool-fs-search
      name: '@greeneek/gnk-tool-fs-search'
      config:
        sampleOverCapGlobResults: false

    - id: agent-instructions
      name: '@greeneek/gnk-agent-instructions'
      config:
        maxBytes: 65536

    - id: skill
      name: '@greeneek/gnk-skill'

    - id: skill-filesystem
      name: '@greeneek/gnk-skill-filesystem'

    - id: skill-badge
      name: '@greeneek/gnk-skill-badge'
      disabled: true

    - id: tool-skill
      name: '@greeneek/gnk-tool-skill'

    - id: commands
      name: '@greeneek/gnk-commands'

    - id: command-feedback
      name: '@greeneek/gnk-command-feedback'

    - id: goal
      name: '@greeneek/gnk-goal'

    - id: goal-round-driver
      name: '@greeneek/gnk-goal-round-driver'

    - id: command-goal
      name: '@greeneek/gnk-command-goal'

    - id: plan-mode
      name: '@greeneek/gnk-plan-mode'
      config:
        section: |
              You are in plan mode. Stay in plan mode until exit_plan_mode succeeds or the user switches the session mode. Imperative language to implement changes means plan the implementation, not execute it. A user's conversational agreement — including an answer confirming something you asked — approves nothing and does not end plan mode; fold the confirmed decision into the plan and submit it through exit_plan_mode.

              Explore first. Use non-mutating reads, searches, static analysis, and checks to ground the plan in the actual repository. Do not edit or write files, change configuration, run formatters or code generation that rewrites tracked files, commit, or otherwise carry out the plan. Prefer existing functions and patterns over new machinery.

              The tool catalog stays the same across modes for request-cache stability. These plan-mode rules override any later tool description or guidance that suggests using mutation tools; those tools remain listed only to keep the request shape stable. Do not use todo_write to track this planning phase: it tracks implementation after an approved plan, while the plan itself belongs in exit_plan_mode.

              Resolve discoverable facts by inspection. Use ask_user_question only for user-owned choices or material ambiguity that inspection cannot answer. Do not ask the user where code lives or how current behavior works when you can find out.

              Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify public API, schema, and data-flow changes; cover edge cases, failure modes, tests, acceptance criteria, and explicit assumptions. Keep it concise enough to review but detailed enough that another engineer can implement it without making design decisions.

              When ready, call exit_plan_mode with the complete plan markdown, starting with a # title. Make exit_plan_mode the only and final tool call in that assistant response: it presents the plan for approval, and implementation begins only in a later step after approval. Do not paste the final plan as a plain reply or ask "should I proceed?" through prose or ask_user_question. If review rejects it, incorporate the feedback and present again. If the review channel is unavailable or aborted, stay in plan mode and ask the user to switch modes manually; do not proceed with implementation.

    - id: token-meter
      name: '@greeneek/gnk-token-meter'

    - id: compaction-basic
      name: '@greeneek/gnk-compaction-basic'

    # Human `/compact`: one useful reduction below the automatic threshold. Backend
    # independent, so it follows whichever compaction service this leaf mounts.
    - id: command-compact
      name: '@greeneek/gnk-command-compact'

    - id: subagent
      name: '@greeneek/gnk-subagent'

    - id: subagent-spawn-in-process
      name: '@greeneek/gnk-subagent-spawn-in-process'
      config:
        providerName: spawn

    - id: subagent-fork-in-process
      name: '@greeneek/gnk-subagent-fork-in-process'
      config:
        providerName: fork

    # Continuable background children are selected per delegation tool. The
    # separately loaded follow-up tool registers the one global `send_message`.
    - id: tool-subagent-control
      name: '@greeneek/gnk-tool-subagent-control'

    - id: tool-subagent-list-agents
      name: '@greeneek/gnk-tool-subagent-control/list-agents'

    - id: tool-subagent
      name: '@greeneek/gnk-tool-subagent'
      config:
        provider: spawn
        toolName: subagent
        backgroundMode: continuable

    # Fork omits model selection so provider/model stay equal to the parent and
    # the inherited history remains eligible for KV Cache reuse. This base row
    # stays one-shot; preset layers may select continuable mode without adding a
    # child-only system-prompt section or tool schema ahead of that history.
    # See .agents/notes/implemented/feature/2026-08-18-model-selected-subagent-routes.md
    # and .agents/notes/implemented/architecture/2026-08-10-fork-children-stay-one-shot.md.
    - id: tool-subagent-fork
      name: '@greeneek/gnk-tool-subagent'
      config:
        provider: fork
        toolName: subagent_fork
        backgroundMode: one-shot

    - id: workflow-worker-thread
      name: '@greeneek/gnk-workflow-worker-thread'
      config:
        provider: spawn

    - id: tool-workflow
      name: '@greeneek/gnk-tool-workflow'

    - id: timeout-policy
      name: '@greeneek/gnk-tool-call-timeout-policy'

    - id: spill-local
      name: '@greeneek/gnk-spill-local'

    - id: spill-policy
      name: '@greeneek/gnk-spill-policy'
      config:
        maxInlineBytes: 50000

    # Durability checkpoints before each model request and top-level dispatch.
    - id: session-checkpoint-policy
      name: '@greeneek/gnk-session-checkpoint-policy'

    # Compacts oversized tool results before the broader conversation compactor
    # runs, preserving the model-visible result within the configured budget.
    - id: tool-result-pruner
      name: '@greeneek/gnk-compaction-tool-result-pruner'
      config:
        thresholdChars: 8192
        headChars: 4096
        tailChars: 1024

    - id: tool-todo
      name: '@greeneek/gnk-tool-todo'
      config:
        allowParallelInProgress: true

    # Persisted same-session goals reach the model and the slash menu here; the
    # domain, driver, and `/goal` command are above.
    - id: tool-goal
      name: '@greeneek/gnk-tool-goal'

    # Fresh-agent Ralph iteration over a build-time-fixed script.
    - id: tool-ralph
      name: '@greeneek/gnk-tool-ralph'
      config:
        subagentProvider: spawn
        maxRounds: 64

    - id: tool-str-replace-editor
      name: '@greeneek/gnk-tool-str-replace-editor'
      config:
        maxOutputChars: 16000

    # Consecutive-repeat reminders on the tool chain.
    - id: repeat-tool-reminder
      name: '@greeneek/gnk-repeat-tool-reminder'
      config:
        thresholds: [3, 5, 8]
        argumentsPreviewChars: 500

    # The shared base enables the stable model-facing web_search and web_fetch
    # tools. The Web app disables this host row and composes both tools per agent
    # preset; products with a stricter network policy override tool-web.
    # Anonymous fetch accepts only public HTTP(S) destinations, resolves and
    # validates every destination, and pins every actual connection.
    #
    # No search provider is pinned: the Greeneek search route talks to
    # `api.greeneek.dev`, which no deployment of this repository operates, so
    # naming it here made every web_search fail against a host that does not
    # resolve. `gnk-web` auto-selects when exactly one usable search provider is
    # registered, so mounting one (Exa or Perplexity below, with its key) is all
    # a deployment needs; with none, `web_search` reports that it is
    # unconfigured while `web_fetch` keeps working.
    - id: web
      name: '@greeneek/gnk-web'
      config:
        fetchProvider: http

    - id: web-fetch-http
      name: '@greeneek/gnk-web-fetch-http'

    # Both search providers mount unconditionally and report themselves
    # unavailable until their key is present ($EXA_API_KEY, $PERPLEXITY_API_KEY,
    # $GOOGLE_SEARCH_API_KEY + $GOOGLE_SEARCH_ENGINE_ID), so an unkeyed
    # deployment registers no usable keyed provider and auto-selection stays
    # unambiguous. DuckDuckGo mounts alongside them and needs no key, so search
    # works out of the box; setting a keyed provider's keys is the case that
    # needs `searchProvider` above (or Settings → General) to name the winner.
    - id: web-search-exa
      name: '@greeneek/gnk-web-search-exa'

    - id: web-search-perplexity
      name: '@greeneek/gnk-web-search-perplexity'

    - id: web-search-google
      name: '@greeneek/gnk-web-search-google'

    - id: web-search-duckduckgo
      name: '@greeneek/gnk-web-search-duckduckgo'

    # The Custom engine vehicle: an Anthropic-compatible Messages endpoint the
    # user points at their own search backend. Unusable until its endpoint and
    # key are configured, so it never disturbs auto-selection before then.
    - id: web-search-greeneek
      name: '@greeneek/gnk-web-search-greeneek'

    # Search is a full auxiliary model request with server-side retrieval for
    # some providers, so the tool keeps a 60s ceiling over the provider-neutral
    # 30s default.
    - id: tool-web
      name: '@greeneek/gnk-tool-web'
      config:
        fetch: true
        searchTimeoutMs: 60000

    # ── rows every mode mounts, whose values each overlay may state ──────────────

    # The tool registry. Presentation mode is a deployment choice; omitting it here
    # keeps the schema default (native).
    - id: tools
      name: '@greeneek/gnk-tools'

    # The deployment persona is a deployment choice; plan-mode and tool plugins own
    # their own prompt sections.
    - id: system-prompt
      name: '@greeneek/gnk-system-prompt'
      config:
        persona: ''

    # Agents created at startup. The base stays empty; raw overlays may create
    # agents, while Web creates sessions on client request.
    - id: agent-loop
      name: '@greeneek/gnk-agent-loop'
      config:
        agents: []

    # The sandboxed filesystem provider. `cwd` defaults to `process.cwd()`; an
    # overlay can pin another workspace.
    - id: fs-sandbox
      name: '@greeneek/gnk-fs-sandbox'

    # The native Greeneek adapter is NOT mounted here. It speaks to
    # `api.greeneek.dev`, which no deployment of this repository operates, so
    # shipping it would put a route in the model picker that can never answer a
    # request and make it the default every new session starts on. Providers
    # come from the user's own keys through `llm-pi-ai` above; a deployment that
    # does run a Greeneek-protocol endpoint mounts the adapter itself:
    #
    #   - insert:
    #       - id: llm-greeneek
    #         name: '@greeneek/gnk-llm-greeneek'
    #         config:
    #           baseURL: https://your-endpoint.example
