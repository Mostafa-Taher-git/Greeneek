/** Host loader entry for the browser-only search-engine preference plugin. */

/** Provides no host-side behavior: the `web` namespace is registered by the web service itself. */
export function apply(): void {}
