[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "greeneek-harness-sdk"
version = "0.0.0.dev0"
description = "Python SDK for Greeneek Harness"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Greeneek" }]
dependencies = [
  "pydantic>=2.12,<3",
  "greeneek-harness-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/greeneek/greeneek-harness"
Documentation = "https://github.com/greeneek/greeneek-harness/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/greeneek/greeneek-harness/issues"
Source = "https://github.com/greeneek/greeneek-harness"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/greeneek_harness"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
greeneek-harness-runtime-bin = { path = "../sdk-runtime", editable = true }
